# Environment variables for the application
# Copy this file to .env and fill in your own values

# ============================================
# APPLICATION SETTINGS
# ============================================

# Ping message for API testing
PING_MESSAGE="ping pong"

# Site URL for Supabase auth redirects (use your local dev URL)
VITE_SITE_URL=http://localhost:8080

# Builder.io public key (optional - get from https://www.builder.io/c/docs/using-your-api-key)
VITE_PUBLIC_BUILDER_KEY=your_builder_public_key_here

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Production Database URL (PostgreSQL)
# Format: postgresql://username:password@host:port/database
# Example for local: postgresql://postgres:postgres@localhost:5432/fairdatause
# Example for Neon: postgresql://user:pass@host.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fairdatause

# Test Database URL (used during test runs)
# Leave empty to auto-start Docker container, or provide your own test database
# Format: postgresql://username:password@host:port/database_test
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fairdatause_test

# ============================================
# REDDIT API CREDENTIALS
# ============================================

# Get these from: https://www.reddit.com/prefs/apps
# 1. Go to https://www.reddit.com/prefs/apps
# 2. Click "Create App" or "Create Another App"
# 3. Fill in:
#    - name: your app name (e.g., "FairDataUse Dev")
#    - type: select "script"
#    - description: optional
#    - about url: optional
#    - redirect uri: http://localhost:8080 (required but not used for script apps)
# 4. Click "Create app"
# 5. Copy the client ID (under the app name) and secret

REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here

# ============================================
# TESTING CONFIGURATION
# ============================================

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# NOTES
# ============================================

# - Do NOT commit your actual .env file with real credentials
# - The .env file is gitignored for security
# - For tests, you can either:
#   1. Set TEST_DATABASE_URL to use your own test database
#   2. Leave it empty and let the test scripts auto-start a Docker container
#   3. Use Testcontainers (requires Docker to be running)
# - For CI/CD, set these as environment variables or secrets